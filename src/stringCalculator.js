// function add(numbers) {
//   if (numbers === "") return 0;
// }

// module.exports = add;
function add(numbers) {
  if (numbers === "") return 0;
  return parseInt(numbers);
}

module.exports = add;
